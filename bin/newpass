#!/usr/bin/perl
if ( ! $ARGV[0] ) {
  print "Usage: ./newpass {password}\n";
  exit 1;
}

@paspts=split(//,$ARGV[0]);
$pasptst=1;
foreach $pasprt(@paspts) {
  $pasptst=$pasptst+ord($pasprt);
}
print crypt($ARGV[0],$pasptst)."\n";
